---
import Welcome from '../components/Welcome.astro';
import Megamenu from '../components/megamenu.astro';
import Cta from '../components/cta.astro';
import Units from '../components/units-appliances-los-angeles.astro';
import Layout from '../layouts/Layout.astro';

// Welcome to Astro! Wondering what to do next? Check out the Astro documentation at https://docs.astro.build
// Don't want to use any of this? Delete everything in this file, the `assets`, `components`, and `layouts` directories, and start fresh.
---
   
<Layout>

  <main>
	<div class="tabs-container">
		<!-- Scroll Hint -->
		<div class="scroll-hint">Swipe or scroll to see more options</div>
		<div class="tabs">
		  <div class="tab" data-target="content1">Air Conditioner Repair</div>
		  <div class="tab" data-target="content2">Cooktop Repair</div>
		  <div class="tab" data-target="content3">Dishwasher Repair</div>
		  <div class="tab" data-target="content4">Dryer Repair</div>
		</div>
		<div class="tab-content">
		
	
		<!-- Content Section -->
		<div class="tab-content">
		  <div id="content1" class="content active">
			<h2>Air Conditioner Repair</h2>
			<img src="air-conditioner.jpg" alt="Air Conditioner" class="tab-image">
			<p>
			  Air conditioners are probably one of man’s greatest inventions. To keep them working properly,
			  regular maintenance is required.
			</p>
			<button>Fix My Air Conditioner</button>
		  </div>
	
		  <div id="content2" class="content">
			<h2>Cooktop Repair</h2>
			<img src="cooktop.jpg" alt="Cooktop" class="tab-image">
			<p>Cooktops require regular maintenance to ensure optimal performance.</p>
			<button>Fix My Cooktop</button>
		  </div>
	
		  <div id="content3" class="content">
			<h2>Dishwasher Repair</h2>
			<img src="dishwasher.jpg" alt="Dishwasher" class="tab-image">
			<p>A properly functioning dishwasher saves time and energy.</p>
			<button>Fix My Dishwasher</button>
		  </div>
	
		  <div id="content4" class="content">
			<h2>Dryer Repair</h2>
			<img src="dryer.jpg" alt="Dryer" class="tab-image">
			<p>Dryers need periodic checks and maintenance to ensure efficiency.</p>
			<button>Fix My Dryer</button>
		  </div>
		</div>
	  </div>
	  <style>
/* General Reset */
body {
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 0;
  background-color: #f7f7f7;
}

/* Tabs Container */
.tabs-container {
  display: flex;
  max-width: 1200px;
  margin: 20px auto;
  border: 1px solid #ccc;
  border-radius: 5px;
  overflow: hidden;
  background-color: #ffffff;
}

/* Tabs Section */
.tabs {
  flex: 1;
  background-color: #003366;
  color: white;
  display: flex;
  flex-direction: column;
  padding: 0;
}

.tab {
  padding: 15px;
  cursor: pointer;
  text-align: left;
  border-bottom: 1px solid #004080;
  transition: background-color 0.3s ease;
}

.tab:hover {
  background-color: #00509e;
}

.tab.active {
  background-color: #004080;
}

/* Content Section */
.tab-content {
  flex: 3;
  padding: 20px;
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
  background-color: #ffffff;
}

.content {
  display: none;
}

.content.active {
  display: block;
}

/* Content Styling */
.tab-image {
  width: 100%;
  max-width: 400px;
  height: auto;
  margin-bottom: 20px;
}

button {
  padding: 10px 20px;
  background-color: #004080;
  color: white;
  border: none;
  border-radius: 5px;
  cursor: pointer;
}

button:hover {
  background-color: #00509e;
}

/* Responsive Design */
@media (max-width: 768px) {
  .tabs-container {
    flex-direction: column;
  }

  .tabs {
    flex-direction: row;
    overflow-x: auto;
    white-space: nowrap;
    border-bottom: 1px solid #ccc;
  }

  .tab {
    flex: 1;
    text-align: center;
    border-bottom: none;
    border-right: 1px solid #004080;
  }

  .tab:last-child {
    border-right: none;
  }

  .tab-content {
    padding: 15px;
  }
}

/* Add a gradient fade at the edges to show scrollability */
.tabs-container {
  position: relative;
}

.tabs::before,
.tabs::after {
  content: "";
  position: absolute;
  top: 0;
  bottom: 0;
  width: 30px;
  pointer-events: none;
  z-index: 1;
}

.tabs::before {
  left: 0;
  background: linear-gradient(to right, rgba(0, 0, 0, 0.2), transparent);
}

.tabs::after {
  right: 0;
  background: linear-gradient(to left, rgba(0, 0, 0, 0.2), transparent);
}

/* Make the scrollbar visible */
.tabs {
  flex-direction: row;
  overflow-x: auto;
  white-space: nowrap;
  border-bottom: 1px solid #ccc;
  scroll-behavior: smooth;
  -webkit-overflow-scrolling: touch; /* For better mobile scrolling */
}

.tabs::-webkit-scrollbar {
  height: 6px;
}

.tabs::-webkit-scrollbar-thumb {
  background: #ccc;
  border-radius: 3px;
}

.tabs::-webkit-scrollbar-track {
  background: transparent;
}

/* Add partial overflow to show more tabs */
.tab {
  flex: 1 0 auto; /* Prevent tabs from shrinking completely */
  min-width: 120px; /* Ensure each tab is wide enough */
  text-align: center;
  border-bottom: none;
  border-right: 1px solid #004080;
}

.tab:last-child {
  border-right: none;
}

/* Hint Text */
.scroll-hint {
  text-align: center;
  font-size: 12px;
  color: #555;
  margin-bottom: 5px;
  display: none;
}

/* Show the hint text on mobile screens */
@media (max-width: 768px) {
  .scroll-hint {
    display: block;
  }
}

	  </style>
	  <script>
// script.js
document.addEventListener("DOMContentLoaded", () => {
  const tabs = document.querySelectorAll(".tab");
  const contents = document.querySelectorAll(".content");

  tabs.forEach((tab) => {
    tab.addEventListener("click", () => {
      // Remove active class from all tabs and contents
      tabs.forEach((t) => t.classList.remove("active"));
      contents.forEach((c) => c.classList.remove("active"));

      // Add active class to the clicked tab and corresponding content
      tab.classList.add("active");
      const target = tab.getAttribute("data-target");
      document.getElementById(target).classList.add("active");
    });
  });
  
});

// Smoothly scroll the tabs to the right when the page loads
document.addEventListener("DOMContentLoaded", () => {
  const tabs = document.querySelector(".tabs");
  setTimeout(() => {
    tabs.scrollBy({ left: 50, behavior: "smooth" });
  }, 1000); // Delay to ensure the user notices
});


	  </script>
    
  </main>

</Layout>